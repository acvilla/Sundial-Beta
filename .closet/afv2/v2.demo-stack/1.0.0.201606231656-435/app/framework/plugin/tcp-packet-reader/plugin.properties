name=TCP Packet Reader

category=IP

released=thin-gateway

qualityString=Production Ready
quality=production

description=This file implements a packet parser on top of TCP.  Since TCP is fundamentally a STREAM oriented protocol, we cannot just assume a call to read() or recv() will return a complete packet of the higher-level protocol.  Therefore this API will make sure a full packet is returned.

sourceFiles=tcp-packet-reader.c
